{
  "name": "chatos-image-workflows",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:workflows": "node scripts/build-workflows.mjs",
    "test": "node --test",
    "validate": "npm run build:workflows && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
